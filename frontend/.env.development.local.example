# Copia este archivo a .env.development.local (no se sube a git):
#   cp .env.development.local.example .env.development.local
#
# Desarrollo: API en tu máquina (arranca el backend con npm run dev en la raíz).
VUE_APP_API_URL=http://localhost:3000

# Opcional: apuntar al backend de Render desde local (requiere CORS en Render).
# VUE_APP_API_URL=https://horarios-20ey.onrender.com
