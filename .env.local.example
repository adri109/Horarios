# Copia a .env.local en la raíz del proyecto (no se sube a git):
#   cp .env.local.example .env.local
#
# Ajustes solo para tu máquina. Render ignora estos archivos y usa su panel de env.

NODE_ENV=development
PORT=3000

# No pongas aquí URLs de producción para CORS; en desarrollo localhost se permite automáticamente.
